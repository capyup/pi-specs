# Third Party Notices

## @tintinweb/pi-tasks

Parts of the built-in task manager under `src/tasks/` are adapted from `@tintinweb/pi-tasks` by tintinweb.

- Upstream: https://github.com/tintinweb/pi-tasks
- License: MIT

The code is vendored/adapted so `pi-spec-driven-dev` can provide task tracking as a first-class local workflow capability without requiring users to install a separate task package.
